{
  "name": "visualization",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./build && ./node_modules/.bin/node-gyp configure && ./node_modules/.bin/node-gyp build --debug",
    "build-xc": "rm -rf ./build && ./node_modules/.bin/node-gyp configure -- -f xcode",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "monkingxue",
  "license": "MIT",
  "devDependencies": {
    "bindings": "^1.2.1",
    "node-gyp": "^3.6.0"
  },
  "dependencies": {
    "animate.css": "^3.5.2",
    "jquery": "^3.2.1",
    "normalize.css": "^6.0.0"
  }
}
